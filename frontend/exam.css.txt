/* frontend/assets/css/exam.css */
/* Styles for the live exam interface and exam creation interface */
.exam-header {
    background-color: var(--primary-color, #2c3e50); /* Fallback if Member 6's CSS fails */
    color: white;
    padding: 15px 0;
    position: sticky;
    top: 0;
    z-index: 100;
}
.timer {
    font-size: 1.2rem;
    font-weight: bold;
    color: var(--warning, #f1c40f);
}
.question-card {
    background: white;
    padding: 20px;
    margin-top: 20px;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    border-left: 5px solid var(--primary-color, #3498db);
}
.question-card h3 {
    margin-bottom: 15px;
    color: #333;
}
.btn-submit {
    width: 100%;
    font-size: 1.1rem;
    padding: 15px;
    background-color: var(--success, #2ecc71);
}